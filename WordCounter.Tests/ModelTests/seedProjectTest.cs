using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace seedProject.Tests
{
  [TestClass]
  public class seedProjectTest
  {
    [TestMethod]
        public void CHANGEME()
        {
          seedProject testSeedProject = new seedProject();
          Assert.AreEqual(false, testSeedProject.CHANGEME());
        }
  }
}
